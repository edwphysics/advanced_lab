## Notes about the challenge

**Goal:** Predict the quasar continua from Lyman-alpha forest spectra

1. Use the models that use DESI for create the continua already filtered
2. X=(z,m,...) Y=continuum spectra 
3. We want to give to the NN a new set of parameters X and predict the Y=continuum that corresponds